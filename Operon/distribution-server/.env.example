# Environment variables for Operon App Distribution Server

# Server Configuration
PORT=3000
NODE_ENV=development

# Admin Authentication
# Change this to a strong, unique secret key in production
ADMIN_KEY=operon-secret-admin-key-change-this

# Optional: Database URL (for future enhancement)
# DATABASE_URL=mongodb://...

# Optional: Logging
LOG_LEVEL=info

# Optional: External APK hosting (recommended for large files)
# Use one of the following:
# - APK_BASE_URL (any HTTPS base URL)
# - GCS_PUBLIC_BASE_URL (public bucket URL)
# - GCS_BUCKET_NAME (will use https://storage.googleapis.com/<bucket>/<file>)
APK_BASE_URL=
GCS_PUBLIC_BASE_URL=
GCS_BUCKET_NAME=
