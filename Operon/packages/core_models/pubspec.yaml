name: core_models
description: Shared models and entities for Dash apps.
version: 0.1.0
publish_to: none
environment:
  sdk: '>=3.2.0 <4.0.0'
dependencies:
  # Allow apps to choose their FlutterFire major (4.x or 5.x).
  cloud_firestore: ">=4.15.5 <6.0.0"
  firebase_auth: ">=4.19.5 <6.0.0"
dev_dependencies:
  flutter_lints: ^3.0.0
