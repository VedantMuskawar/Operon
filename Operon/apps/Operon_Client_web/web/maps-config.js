/**
 * Google Maps API Key Configuration
 * 
 * This file is loaded before the Google Maps script to provide the API key.
 * 
 * For development:
 * - Set window.GOOGLE_MAPS_API_KEY before this script loads, or
 * - Replace the value below with your API key
 * 
 * For production:
 * - Use the build script: node scripts/replace-maps-api-key.js [API_KEY]
 * - Or set GOOGLE_MAPS_API_KEY environment variable during build
 */

// API key can be set here, via environment variable during build, or via window variable
window.GOOGLE_MAPS_API_KEY = window.GOOGLE_MAPS_API_KEY || 'AIzaSyA_uQM3BwYUtAWyRddkWX9Cfajixv7szYc';
